# 更新日志

- **[2026-09-02] feat(k8s): components/k8s 新增 kubelet cAdvisor 指标拉取封装（pod-cpu-cadvisor，v0.3.1 待发布）**。feature/pod-cpu-cadvisor。为 homelab-backend PodMetricCollector 的 CPU 数据源改造提供基础能力：CPU 从 metrics-server 的约 1 分钟预均值快照（PodMetrics）换成 kubelet cAdvisor 的累计计数器（原样入库，速率由查询侧下期按 non_negative_derivative 计算）。`components/k8s/cadvisor.go` 新增：
  - `GetPodCAdvisorUsage(ctx)`：CoreV1().Nodes().List 列节点 → errgroup 有界并发（`SetLimit(8)`）逐节点经 apiserver proxy（`GET /api/v1/nodes/{node}/proxy/metrics/cadvisor`，复用 Client 的 rest.Config / SA token / TLS，不直连 kubelet；`Stream` 流式读取响应体并经 64MB 上限 reader 喂给解码，超限即报错）→ 解析 Prometheus 文本 → 按 pod 维度（namespace/pod）聚合业务容器用量，返回 `map[PodRef]PodCAdvisorUsage`（`PodRef{Namespace, Pod}` 结构化 key，不做复合字符串 key，调用方按字段取用），用量为 `PodCAdvisorUsage{CPUSecondsTotal float64, MemoryWorkingSetBytes int64}`。任一节点抓取/解析失败即取消其余在途请求并 fast-fail 返回 error（errgroup.WithContext 传播，总超时由调用方 ctx 控制，调用方整体退避重试、不做部分写入）。各 goroutine 聚合进独立 map、Wait 后单协程合并，无共享写。
  - `aggregateCAdvisorPodUsage(r io.Reader, acc)`：纯函数解析与聚合，仅关注 `container_cpu_usage_seconds_total`（counter，秒）与 `container_memory_working_set_bytes`（gauge，字节）两个指标；只聚合带 namespace+pod 标签且 `container != "POD"` 的业务容器 series，过滤 POD 虚拟容器（pause/infra，份额与业务容器重复）与无 pod 标签的 node/系统组件 series；NaN 样本跳过（NaN 会让下游 json.Marshal 报错引发整轮失败）；计数器回绕（pod 重启归零）不在采集侧处理，由查询侧 non_negative_derivative 兜底。既有 `GetPodMetrics`/`AggregatePodMetrics`（metrics-server 封装）保留不动，cluster_monitor 等下游不受影响。
  - **改动文件**：`components/k8s/cadvisor.go`（新增）、`components/k8s/cadvisor_test.go`（新增，样例文本覆盖 POD 虚拟容器排除、无 pod 标签 series 排除、多容器累加、科学计数法、NaN 跳过、空文本/非法文本 fast-fail、注入假 fetch 的并发成功合并/任一节点抓取与解析失败整轮失败、maxBytesReader 恰好读满/超限报错）、`go.mod`（`golang.org/x/sync v0.20.0`、`prometheus/common v0.67.5`、`prometheus/client_model v0.6.2` 由 indirect 转直接依赖，版本均不变，零新增模块）。
  - **取舍**：Prometheus 文本解析选用 `expfmt` 而非手写——cAdvisor 单节点输出数百指标数千行，expfmt 按 MetricFamily 解码且非关注 family 直接跳过，同时规避手写解析的转义/科学计数法边界（注意 expfmt v0.67.5 文本解码为全量解析，流式仅体现在 HTTP 读取层：`Stream` 替代 `Do().Raw()` 的整体 ReadAll，避免响应体双份内存，64MB 上限由自实现 `maxBytesReader` 兜底——`io.LimitReader` 到限静默 EOF 无法表达「超限报错」语义）；节点抓取 errgroup 有界并发 8（单轮 10s 总超时内串行逐节点在大集群有超时风险，并发把墙钟时间压到约 max 单节点耗时；任一失败取消在途请求的语义由 WithContext 天然满足）；NaN 防御在采集侧跳过（encoding/json 对 NaN 直接报错）；AbsPath 不做路径转义，node 仅来自 Nodes().List（apiserver 已校验 DNS-1123），代码注释标明禁止传入外部输入。API 形态沿用「单 Client struct + 直白方法 + 域内纯函数可测」风格，不建接口不建工厂（fetch 以函数参数注入，不建接口）。
  - **验证**：`go build ./components/k8s/...` / `go vet ./components/k8s/...` 通过；`go test -race -count=1 -run 'TestAggregate|TestFetch|TestMaxBytes' ./components/k8s/` 全 PASS（含 -race 下并发聚合无数据竞争；既有 `TestClient` 需 `/root/.kube/config`，无该文件时 panic 为基线问题，stash 验证与本次无关；`go build ./...` 仅 `components/collect` 失败亦为既有基线，见 v0.2.6 条目）。

- **[2026-08-16] fix(tsdb): Flux 查询规则修正（fix-metric-tzdata，v0.2.7）**。feature/fix-metric-tzdata。真实根因（容器无 tzdata 导致 `time.LoadLocation("Asia/Shanghai")` 失败返回 nil、用户输入被回退解析为 UTC → 查询窗口偏移 +8h）由 **homelab-backend** 根治：全部 7 个服务 import `_ "time/tzdata"` 内置 zoneinfo，真实 tzdata 下 `DefaultTimeZone` 不再为 nil。go-tools **不引入 FixedZone 兜底**（与 homelab 修复重叠，且固定 +08:00 偏移不如真实 zoneinfo 彻底），本版本仅含 Flux 查询规则修正与微优化：
  - **`components/tsdb/def/tsdb_query.go`**：① 无 tag 分组时 `group(columns: [])` → `group(columns: ["_field"])`，保留字段身份 → `record.Field()` 不再为空 → series 名恢复为真实字段名（非空 case 已含 `_field`，规则统一）；② `aggregateWindow(every: %s, fn: mean)` → 追加 `createEmpty: false`，避免空窗口 null 行。
  - **`components/tsdb/internal/influxdb.go`**：微优化 `field := record.Field(); if field == ""`；`keys` 预分配容量 `len(options.Groups)*len(options.Fields)` → `len(options.Groups)+1`（keys 实际为 groups + field）；series 组装循环提取为可独立测试的 `parseQueryResults(options, results)`，`Query` 仅做查询构造后委托（行为零变更）。
  - **测试**：`components/tsdb/def/tsdb_query_test.go` 新增 group 空 → `group(columns: ["_field"])`（含非空 case 含 `_field`）与 aggregateWindow `createEmpty: false` 断言；新增响应级测试 `components/tsdb/internal/query_parse_test.go`——用 influxdb-client-go 的 `api.NewQueryTableResult` 喂入含 `#group/#default` 注解与 `_field` 列的模拟 InfluxDB CSV 响应，走 `parseQueryResults` 组装逻辑，断言 series 名/Columns 为真实字段名 `usage` 而非 `default_count`（覆盖无 tag 分组/含 tag 分组/多字段三场景，并固化缺 `_field` 时回退 `default_count` 的失败模式）。既有断言均为 UTC Z 字面量，无旧 `group(columns: [])`/无 `createEmpty` 断言需同步。
  - **测试基线修复**：`components/tsdb/internal/influxdb_test.go` 两处既有失败（master 上同样存在）——`TestReadMetric` 补 `INFLUXDB_*` 环境变量 skip guard（原无 env 时 `NewInfluxdbClient` 返回 nil client，defer `Close()` panic 并中止整包后续测试）；`TestNewDefaultCat/validates_org` 改用 dummy endpoint `http://localhost:8086`（原 endpoint 为空时 `validate()` 先返回 endpoint 错误，断言永远不达 org 分支）。
  - **取舍**：`group` 空 case 不直接去掉分组（`group()` 无参会按 `_field` 分组但显式声明更清晰），与调用方「合并 tag 保留字段」的意图一致。
  - **验证**：`go build ./components/tsdb/...` / `go vet ./components/tsdb/...` 通过；`go test -race -count=1 ./components/tsdb/...` 全 PASS（新增响应级断言，且基线修复后整包不再被 `TestReadMetric` panic 中止）。

- **[2026-08-16] fix(tsdb): Flux range 显式时间转 UTC 输出 Z 字面量，修复跨时区窗口偏移（fix-metric-window，v0.2.6）**。feature/fix-metric-window。`components/tsdb/def/tsdb_query.go` 的 Flux 模板 `range(start: %s, stop: %s)` 中时间字面量原带 `+08:00` 偏移，被 Flux 按 UTC 解析导致查询窗口整体偏移 +8h（「近 6 小时/近 1 小时」窗口落入未来无数据、曲线消失；「近 24 小时」因跨度大仍重叠历史数据）。此前曾尝试对 RFC3339 字面量加双引号，但 flux 的 `range` 参数经 `args.GetRequiredTime` → `flux.ToQueryTime` 解析只接受 Time/Duration/Int，字符串直接 400 `value is not a time, got string`，故放弃方案 A 改为方案 B：`getTimeRange()` 对显式 Start/End 先 `UTC()` 再 `Format(time.RFC3339)`，产出无引号 Z 结尾字面量（如 `2026-08-16T03:34:27Z`），`now()`/`-1m` 等 Flux 关键字与相对时长保持原样不加引号不转 UTC。`components/tsdb/internal/influxdb.go` fallback 系列名 `deafult_count` → `default_count`（typo 修正）。
  - **改动文件**：`components/tsdb/def/tsdb_query.go`（`getTimeRange()` 显式时间转 UTC 输出 Z 字面量）、`components/tsdb/def/tsdb_query_test.go`（`TestGetQueryRangeTimeLiteral` 断言改为 UTC+Z 无引号，`ParseInLocation` 改用 `time.FixedZone("CST", 8*3600)` 去除系统 tzdata 依赖，并补「仅设 End」分支用例断言 `start: -1m` 关键字保持原样）、`components/tsdb/internal/influxdb.go`（typo）。
  - **取舍**：方案 A（字面量加引号）会让 flux 解析器以字符串拒绝入参，所有显式时间查询 400，比原 bug 更糟，弃用。方案 B（转 UTC 输出 Z 字面量）中带偏移的 RFC3339 与其对应 UTC 表示同一时刻，Flux 将无偏移字面量按 UTC 解释后结果与原始时刻一致，同时规避了字符串嗅探的脆弱性；转换内联在 `getTimeRange()` 唯一产出点，`now()`/`-1m` 关键字分支原样透出不受影响，规则「显式时间转 UTC、关键字/相对时长原样」单点覆盖，端到端测试即覆盖。
  - **验证**：`go build ./components/tsdb/...` / `go vet ./components/tsdb/...` 通过；`go test ./components/tsdb/def/` 全 PASS（含 11:34 CST → 03:34 UTC 的 Z 字面量断言）；`go test ./components/tsdb/internal/` 的 `TestReadMetric` 因本地无 `INFLUXDB_*` 环境变量失败为既有基线（该测试独缺 env skip guard，其余用例无 env 自动 skip）。`go build ./...` 的 `components/collect` 包失败亦为既有基线（`monitor.CollectStats` 签名不匹配，与本次改动无关，stash 验证）。
  - **因果修正（v0.2.7）**：本条目修复（UTC Z 字面量输出）方向正确但**非充分**——真实根因是容器无 tzdata 导致 `LoadLocation("Asia/Shanghai")` 失败返回 nil、用户输入被回退解析为 UTC，窗口偏移发生在「时刻解析」阶段而非「字面量产出」阶段。根因由 homelab-backend 修复（全服务 `_ "time/tzdata"`），本条目改动保留（UTC Z 输出对「已正确解析的时刻」仍是必要的正确行为），详见 v0.2.7 条目。

- **[2026-08-15] 2b ponytail 精简（kafka-ops）**：① `components/kafka/admin_client.go` 删除死字段 `TopicMeta.Internal`/`TopicMeta.UnderReplicated` 及 `underReplicated` 辅助函数（下游 deck proto 不消费）；② `NewAdminClient` 复用 `NewKafkaDialer` 替换手写 readerDialer，与 Client/Producer 拨号配置对齐（顺带补上 `DualStack:false` 禁用 IPv6，兼容 SSH 隧道）；③ 抽取 `partitionBounds(parts []kafka.Partition) []kafka.OffsetRequest`，消除 messageCounts/TopicOffsets/ReadLatestMessages 三处重复的 First+Last offset 拼装。`go build ./components/kafka/...` / `go vet ./components/kafka/...` 通过；AdminClient 测试无 broker 环境自动 skip 全绿（既有 kafka_test.go 需真实 broker 的失败为基线问题，stash 验证）。
- **[2026-08-15] feat(kafka): components/kafka 新增 AdminClient 运维封装（kafka-ops）**。feature/kafka-ops。为 homelab 提供 Kafka 信息查询与运维能力，基于 kafka-go 协议层 API（Metadata/ListOffsets/OffsetFetch/DescribeGroups/DescribeConfigs 等），沿用既有 Client/Producer 的 Option 模式（WithAddress/WithSCRAMSASL，SCRAM-SHA-512）。`components/kafka/admin_client.go` 新增：`NewAdminClient(opts...)`（brokers + SASL 走 Transport/Dialer）、`ListTopics`（分区数/副本/ISR 欠副本标记/retention.ms/segment.bytes/消息量估算，批量 API 一次往返）、`TopicOffsets`（per partition leader/highWatermark/logStartOffset）、`ListConsumerGroups`（组名/状态/成员数/协议类型，ListGroups+DescribeGroups 合并）、`ConsumerGroupLag`（per partition currentOffset vs logEndOffset，聚合 totalLag；未提交 offset 记 0）、`ReadLatestMessages`（各分区从最新水位回读 count 条，按时间倒序取最新 count 条）、`CreateTopic`（可选 topic 级 config）/`DeleteTopic`/`DeleteConsumerGroup`。
  - **改动文件**：`components/kafka/admin_client.go`（新增）、`components/kafka/admin_client_test.go`（新增，无 broker 环境探测后自动 skip，避免集成测试硬失败）。
  - **取舍**：管理操作全部走批量 API（一次 Metadata + DescribeConfigs + ListOffsets 完成列表），避免逐 topic 往返；消息量用 `logEndOffset-logStartOffset` 求和估算而非精确计数（成本可控）；`ReadLatestMessages` 以「每分区回读 count 条 + 全局时间倒序取 count」实现多分区最新 N 条，不做严格全局有序保证（运维抽样场景足够）；CreateTopic 直接透出 kafka-go 的 `ConfigEntry`，不另造配置类型。
  - **验证**：`go build ./components/kafka/...` / `go vet ./components/kafka/...` 通过；`go test ./components/kafka/...` 新增用例在无 broker 环境 skip、有 broker 环境全量跑（既有 kafka_test.go/producer_test.go 仍为需真实 broker 的集成测试）。

- **[2026-08-14] fix(kafka): Producer 新增 Close()（add-monitor-page）**。feature/add-monitor-page。为 homelab 采集器优雅停止提供关闭入口：`components/kafka/producer.go` 新增 `Close() error`，关闭底层 `kafka.Writer`（nil writer 幂等返回 nil）。`go build ./components/kafka/...` / `go vet ./components/kafka/...` 通过。
- **[2026-08-14] feat(k8s): components/k8s 新增 metrics-server 客户端封装（add-monitor-page）**。feature/add-monitor-page。为 pod 指标采集提供基础能力：`Client` 增加 `metricsClient` 字段（`metrics.k8s.io/v1beta1` clientset，`NewClient` 时随 `k8s.io/client-go` 一并初始化，仅创建客户端不做实际 API 调用，集群未安装 metrics-server 不会影响构造），新增 `GetPodMetrics(ctx, namespace)`（拉取 PodMetricsList，namespace 传空串表示全命名空间）与 `AggregatePodMetrics(pm)`（按 pod 聚合各容器 CPU/内存，返回 cpuCores 核数 float64 与 memoryBytes 字节数 int64）。新增直接依赖 `k8s.io/metrics v0.35.3`（与现有 `k8s.io/api|apimachinery|client-go v0.35.3` 同版本系列）。
  - **改动文件**：`components/k8s/client.go`（struct 增加 `metricsClient` 字段 + `NewClient` 初始化）、`components/k8s/metrics.go`（新增）、`go.mod`/`go.sum`（新增 `k8s.io/metrics v0.35.3`）。
  - **取舍**：沿用现有 `components/k8s` 的「单 `Client` struct + 直白方法」风格，不引入独立 metrics 客户端接口（单一实现不建接口）；CPU 聚合统一转为 milli-cores 求和后除以 1000 得到核数，避免 float 逐容器累加丢失精度；内存取 `Quantity.Value()` 字节数，与 InfluxDB 落库的数值类型保持一致。
  - **验证**：`go build ./components/k8s/...` / `go vet ./components/k8s/...` 通过；`go mod tidy` 仅新增 `k8s.io/metrics` 一个直接依赖，无其他 go.mod 扰动。
- **[2026-08-14] 2b ponytail 精简（add-monitor-page）**：① 删除 `GetMetricsClient()` getter（与 `GetPodMetrics` 是同一 metrics clientset 的双入口，按「域方法优先、对齐 `GetPod/ListPods` 风格」收敛为单一入口，getter 零消费方）；② `GetPodMetrics` 参数简化：`(ctx, namespace, opts metav1.ListOptions)` → `(ctx, namespace)`（无调用方传非空 opts，缺省 `metav1.ListOptions{}` 内联），同步删除 `metricsClient` 双入口相关的 CHANGELOG 描述。`go build ./components/k8s/...` / `go vet ./components/k8s/...` / `go test ./components/k8s/...` 通过。
